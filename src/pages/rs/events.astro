---
import type { Language, Locale } from "../../bin/types";

import BaseLayout from "@layouts/BaseLayout.astro";
import EventsLayout from "@layouts/EventsLayout.astro";

import eventsData from "../../data/events.json";

const language: Language = "rs";
const locale: Locale = "sr-Latn";
---

<BaseLayout title="Milan Petković" page="Nastupi" language={language}>
  <EventsLayout language={language} events={eventsData} locale={locale} />
</BaseLayout>
