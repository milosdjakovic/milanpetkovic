---
import type { Language } from "../../bin/types";

import BaseLayout from "../../layouts/BaseLayout.astro";
import EventsLayout from "../../layouts/EventsLayout.astro";

import siteData from "../../data/site_data.json";
import eventsData from "../../data/events.json";

const language: Language = "en";
---

<BaseLayout title="Milan Petković" page="Events" language={language}>
  <EventsLayout
    upcomingEventsTitle={siteData.phrases.events.upcoming[language]}
    noEventsTitle={siteData.phrases.events.none[language]}
    pastEventsTitle={siteData.phrases.events.past[language]}
    events={eventsData}
    locale="en"
  />
</BaseLayout>
