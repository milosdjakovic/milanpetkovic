---
import type { Language } from "../../bin/types";

import BaseLayout from "../../layouts/BaseLayout.astro";
import EventsLayout from "../../layouts/EventsLayout.astro";

import i18nData from "../../data/i18n_data.json";
import eventsData from "../../data/events.json";

const language: Language = "en";
---

<BaseLayout title="Milan Petković" page="Events" language={language}>
  <EventsLayout
    upcomingEventsTitle={i18nData.events.upcoming[language]}
    noEventsTitle={i18nData.events.none[language]}
    pastEventsTitle={i18nData.events.past[language]}
    events={eventsData}
    locale="en"
  />
</BaseLayout>
