---
import type { Language } from "../bin/types";

import BaseLayout from "@layouts/BaseLayout.astro";

const language: Language = "en";
---

<BaseLayout title="Milan Petković" page="" language={language} />

<script>
  import type { Language } from "../bin/types";
  import { getLanguage } from "../bin/language";

  const language: Language = getLanguage() ?? "en";
  location.href = `${language}/`;
</script>
