---
import type { AlbumData, Language } from "../bin/types";
import discographyData from "../data/discography.json";

import AlbumList from "../components/AlbumList.astro";

const discography: AlbumData[] = discographyData;

export interface Props {
  title: string;
  language: Language;
}

const { title, language } = Astro.props;
---

<h1 class="text-2xl text-center">{title}</h1>

<AlbumList language={language} discography={discography} />
